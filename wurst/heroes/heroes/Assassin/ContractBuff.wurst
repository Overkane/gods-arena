package ContractBuff
import PresetBuffs
import Assets

public constant buffTuple = compiletime(createDummyBuffObject("Contract Target", "This unit is the target of the Assassin's contract",  Icons.bTNSnazzyScrollPurple, Objects.potofGold, AttachmentPoints.overhead))

public class ContractBuff extends NormalBuff

  construct(real dur)
    super(dur, buffTuple)
    
  override function onApply()

  override function onEnd()
