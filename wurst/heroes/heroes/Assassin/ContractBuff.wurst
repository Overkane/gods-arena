package ContractBuff
import PresetBuffs
import Assets
import MapModels

public constant buffTuple = compiletime(createDummyBuffObject("Contract Target", "This unit is target of the contract",  Icons.bTNSnazzyScrollPurple, MapModels.redSnipeTarget, AttachmentPoints.overhead))

public class ContractBuff extends NormalBuff

  construct(real dur)
    super(dur, buffTuple)
    
  override function onApply()

  override function onEnd()
