package ScrollOfBeast
import ItemObjEditing
import AbilityObjEditing
import ObjectIdGenerator
import ObjectIds

public constant SCROLL_OF_BEAST_ID = compiletime(ITEM_ID_GEN.next())
constant SCROLL_OF_BEAST_ABIL_ID = compiletime(ABIL_ID_GEN.next())

@compiletime function genItems()
  new ItemDefinition(SCROLL_OF_BEAST_ID, 'sror')
  // ..setAbilities(SCROLL_OF_BEAST_ABIL_ID.toRawCode())
