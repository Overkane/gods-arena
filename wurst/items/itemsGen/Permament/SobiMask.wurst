package SobiMask
import ItemObjEditing
import AbilityObjEditing
import ObjectIdGenerator
import ObjectIds

public constant SOBI_MASK_ID = compiletime(ITEM_ID_GEN.next())
constant SOBI_MASK_ABIL_ID = compiletime(ABIL_ID_GEN.next())

@compiletime function genItems()
  new ItemDefinition(SOBI_MASK_ID, 'rwiz')
  // ..setAbilities(SOBI_MASK_ABIL_ID.toRawCode())
